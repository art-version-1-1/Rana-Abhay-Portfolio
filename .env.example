# Copy this to .env and replace with your local or hosted Postgres URL.
# Recommended local development setup (peer auth via Unix socket):
DATABASE_URL=postgresql://rana@/rana_portfolio?host=/var/run/postgresql
# For a hosted provider such as Neon, use your provider URL instead.
